---
title: Advanced matrix 概念 |Microsoft Docs
description: Advanced matrix 概念
author: QuantumWriter
uid: microsoft.quantum.concepts.matrix-advanced
ms.author: nawiebe@microsoft.com
ms.date: 12/11/2017
ms.topic: article
ms.openlocfilehash: f87b3bcd19d2f98fea2a9724a280781a78c4cbb9
ms.sourcegitcommit: 8becfb03eb60ba205c670a634ff4daa8071bcd06
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 10/26/2019
ms.locfileid: "73183755"
---
# <a name="advanced-matrix-concepts"></a><span data-ttu-id="07bde-103">Advanced Matrix 概念</span><span class="sxs-lookup"><span data-stu-id="07bde-103">Advanced Matrix Concepts</span></span> #

<span data-ttu-id="07bde-104">我們現在將矩陣的操作延伸到[*特徵值、特徵向量*](https://en.wikipedia.org/wiki/Eigenvalues_and_eigenvectors)和[*指數*](https://en.wikipedia.org/wiki/Matrix_exponential)，這會形成一組我們所需的基本工具，以描述和執行量子演算法。</span><span class="sxs-lookup"><span data-stu-id="07bde-104">We now extend our manipulation of Matrices to [*Eigenvalues, Eigenvectors*](https://en.wikipedia.org/wiki/Eigenvalues_and_eigenvectors) and [*Exponentials*](https://en.wikipedia.org/wiki/Matrix_exponential) which form a fundamental set of tools we need to describe and implement quantum algorithms.</span></span>

## <a name="eigenvalues-and-eigenvectors"></a><span data-ttu-id="07bde-105">特徵值和特徵向量</span><span class="sxs-lookup"><span data-stu-id="07bde-105">Eigenvalues and Eigenvectors</span></span> ##

<span data-ttu-id="07bde-106">讓 $M $ 成為正方形矩陣，$v $ 是不是全部零向量的向量（亦即，所有專案都等於 $0 $ 的向量）。</span><span class="sxs-lookup"><span data-stu-id="07bde-106">Let $M$ be a square matrix and $v$ be a vector that is not the all zeros vector (i.e., the vector with all entries equal to $0$).</span></span>

<span data-ttu-id="07bde-107">我們說 $v $ 是 $M $ 的[*eigenvector*](https://en.wikipedia.org/wiki/Eigenvalues_and_eigenvectors) ，如果 $Mv = cv $ 代表某個數位 $c $。</span><span class="sxs-lookup"><span data-stu-id="07bde-107">We say $v$ is an [*eigenvector*](https://en.wikipedia.org/wiki/Eigenvalues_and_eigenvectors) of  $M$ if $Mv = cv$ for some number $c$.</span></span> <span data-ttu-id="07bde-108">我們說 $c $ 是對應至 eigenvector $v $ 的[*eigenvalue*](https://en.wikipedia.org/wiki/Eigenvalues_and_eigenvectors) 。</span><span class="sxs-lookup"><span data-stu-id="07bde-108">We say $c$ is the [*eigenvalue*](https://en.wikipedia.org/wiki/Eigenvalues_and_eigenvectors) corresponding to the eigenvector $v$.</span></span> <span data-ttu-id="07bde-109">在一般情況下，矩陣 $M $ 可以將向量轉換成任何其他向量，但 eigenvector 是特殊的，因為它會保留不變，除非乘以數位。</span><span class="sxs-lookup"><span data-stu-id="07bde-109">In general a matrix $M$ may transform a vector into any other vector, but an eigenvector is special because it is left unchanged except for being multiplied by a number.</span></span> <span data-ttu-id="07bde-110">請注意，如果 $v $ 是 eigenvalue $c $ 的 eigenvector，則 $av $ 也是具有相同 eigenvalue 的 eigenvector （適用于任何非零 $a $）。</span><span class="sxs-lookup"><span data-stu-id="07bde-110">Note that if $v$ is an eigenvector with eigenvalue $c$, then $av$ is also an eigenvector (for any nonzero $a$) with the same eigenvalue.</span></span>

<span data-ttu-id="07bde-111">例如，對於識別矩陣，每個向量 $v $ 都是 eigenvalue $1 $ 的 eigenvector。</span><span class="sxs-lookup"><span data-stu-id="07bde-111">For example, for the identity matrix, every vector $v$ is an eigenvector with eigenvalue $1$.</span></span>

<span data-ttu-id="07bde-112">另舉一個例子，請考慮使用[*對角線矩陣*](https://en.wikipedia.org/wiki/Diagonal_matrix)$D $，在對角線上只有非零的專案：</span><span class="sxs-lookup"><span data-stu-id="07bde-112">As another example, consider a [*diagonal matrix*](https://en.wikipedia.org/wiki/Diagonal_matrix) $D$ which only has nonzero entries on the diagonal:</span></span>

<span data-ttu-id="07bde-113">$ $ \begin{bmatrix} d_1 & 0 & 0 \\\\ 0 & d_2 & 0 \\\\ 0 & 0 & d_3 \end{bmatrix}。</span><span class="sxs-lookup"><span data-stu-id="07bde-113">$$ \begin{bmatrix} d_1 & 0 & 0 \\\\ 0 & d_2 & 0 \\\\ 0 & 0 & d_3 \end{bmatrix}.</span></span>
$$

<span data-ttu-id="07bde-114">向量</span><span class="sxs-lookup"><span data-stu-id="07bde-114">The vectors</span></span>

<span data-ttu-id="07bde-115">$ $ \begin{bmatrix}1 \\\\ 0 \\\\ 0 \end{bmatrix}、\begin{bmatrix}0 \\\\ 1 \\\\ 0 \ end {bmatrix} 和 \begin{bmatrix}0 \\\\ 0 \\\\1 \ 結束 {bmatrix} $ $</span><span class="sxs-lookup"><span data-stu-id="07bde-115">$$\begin{bmatrix}1 \\\\ 0 \\\\ 0 \end{bmatrix}, \begin{bmatrix}0 \\\\ 1 \\\\ 0\end{bmatrix} and \begin{bmatrix}0 \\\\ 0 \\\\ 1\end{bmatrix}$$</span></span>

<span data-ttu-id="07bde-116">這是此矩陣的特徵向量，分別具有特徵值 $d _1 $、$d _2 $ 和 $d _3 $。</span><span class="sxs-lookup"><span data-stu-id="07bde-116">are eigenvectors of this matrix with eigenvalues  $d_1$, $d_2$, and $d_3$, respectively.</span></span> <span data-ttu-id="07bde-117">如果 $d _1 $、$d _2 $ 和 $d _3 $ 是相異數位，則這些向量（及其倍數）是矩陣 $D $ 的唯一特徵向量。</span><span class="sxs-lookup"><span data-stu-id="07bde-117">If $d_1$, $d_2$, and $d_3$ are distinct numbers, then these vectors (and their multiples) are the only eigenvectors of the matrix $D$.</span></span> <span data-ttu-id="07bde-118">一般而言，如果是對角矩陣，就很容易閱讀特徵值和特徵向量。</span><span class="sxs-lookup"><span data-stu-id="07bde-118">In general, for a diagonal matrix it is easy to read off the eigenvalues and eigenvectors.</span></span> <span data-ttu-id="07bde-119">特徵值是顯示在對角線上的所有數位，其各自的特徵向量就是一個專案等於 $1 $ 且剩餘的專案等於 $0 $ 的單位向量。</span><span class="sxs-lookup"><span data-stu-id="07bde-119">The eigenvalues are all the numbers appearing on the diagonal, and their respective eigenvectors are the unit vectors with one entry equal to $1$ and the remaining entries equal to $0$.</span></span>

<span data-ttu-id="07bde-120">請注意，在上述範例中，$D $ 的特徵向量會形成 $ 3 $ 維向量的基礎。</span><span class="sxs-lookup"><span data-stu-id="07bde-120">Note in the above example that the eigenvectors of $D$ form a basis for $3$-dimensional vectors.</span></span> <span data-ttu-id="07bde-121">「基礎」是一組向量，可讓任何向量以線性組合的方式寫入。</span><span class="sxs-lookup"><span data-stu-id="07bde-121">A basis is a set of vectors such that any vector can be written as a linear combination of them.</span></span> <span data-ttu-id="07bde-122">更明確地來說，$v _1 $，$v _2 $，以及 $v _3 $ form，如果任何向量 $v $ 可以撰寫成 $v = a_1 v_1 + a_2 v_2 + a_3 v_3 $，$a _2 $ 和 $a _3 $。</span><span class="sxs-lookup"><span data-stu-id="07bde-122">More explicitly, $v_1$, $v_2$, and $v_3$ form a basis if any vector $v$ can be written as $v=a_1 v_1 + a_2 v_2 + a_3 v_3$ for some numbers $a_1$, $a_2$, and $a_3$.</span></span>

<span data-ttu-id="07bde-123">回想一下，Hermitian 矩陣（也稱為自我 adjoint）是一個複雜的方形矩陣，它等於它自己的複數共軛，而單一矩陣則是一個複雜的正方形矩陣，其反向等於複數共軛。</span><span class="sxs-lookup"><span data-stu-id="07bde-123">Recall that a Hermitian matrix (also called self-adjoint) is a complex square matrix equal to its own complex conjugate, while a unitary matrix is a complex square matrix whose inverse is equal to its complex conjugate.</span></span>
<span data-ttu-id="07bde-124">對於 Hermitian 和單一矩陣（基本上是在量子運算中唯一遇到的矩陣），有一個稱為[*spectral 定理*](https://en.wikipedia.org/wiki/Spectral_theorem)的一般結果，其判斷提示如下：針對任何 Hermitian 或單一矩陣 $M $，有一個單一 $U $，使其 $M = U ^ \dagger D U $ 用於某個對角線矩陣 $D $。</span><span class="sxs-lookup"><span data-stu-id="07bde-124">For Hermitian and unitary matrices, which are essentially the only matrices encountered in quantum computing, there is a general result known as the [*spectral theorem*](https://en.wikipedia.org/wiki/Spectral_theorem), which asserts the following: For any Hermitian or unitary matrix $M$, there exists a unitary $U$ such that $M=U^\dagger D U$ for some diagonal matrix $D$.</span></span> <span data-ttu-id="07bde-125">此外，$D $ 的對角線專案會是 $M $ 的特徵值。</span><span class="sxs-lookup"><span data-stu-id="07bde-125">Furthermore, the diagonal entries of $D$ will be the eigenvalues of $M$.</span></span>

<span data-ttu-id="07bde-126">我們已經知道如何計算對角矩陣 $D $ 的特徵值和特徵向量。</span><span class="sxs-lookup"><span data-stu-id="07bde-126">We already know how to compute the eigenvalues and eigenvectors of a diagonal matrix $D$.</span></span> <span data-ttu-id="07bde-127">使用此定理時，我們知道如果 $v $ 是 eigenvector $D $ with eigenvalue $c $，亦即 $Dv = cv $，則 $U ^ \dagger v $ 將是 $M $ with eigenvector $c $ 的 eigenvalue。</span><span class="sxs-lookup"><span data-stu-id="07bde-127">Using this theorem we know that if $v$ is an eigenvector of $D$ with eigenvalue $c$, i.e., $Dv = cv$, then $U^\dagger v$ will be an eigenvector of $M$ with eigenvalue $c$.</span></span> <span data-ttu-id="07bde-128">這是因為</span><span class="sxs-lookup"><span data-stu-id="07bde-128">This is because</span></span>

<span data-ttu-id="07bde-129">$ $M （U ^ \dagger v） = U ^ \dagger D U （U ^ \dagger v） = U ^ \dagger D （U U ^ \dagger） v = U ^ \dagger D v = c U ^ \dagger v $ $</span><span class="sxs-lookup"><span data-stu-id="07bde-129">$$M(U^\dagger v) = U^\dagger D U  (U^\dagger v) =U^\dagger D (U  U^\dagger) v = U^\dagger D v = c U^\dagger v.$$</span></span>

## <a name="matrix-exponentials"></a><span data-ttu-id="07bde-130">矩陣指數</span><span class="sxs-lookup"><span data-stu-id="07bde-130">Matrix Exponentials</span></span>
<span data-ttu-id="07bde-131">[*矩陣指數*](https://en.wikipedia.org/wiki/Matrix_exponential)也可以定義為與指數函數完全相似。</span><span class="sxs-lookup"><span data-stu-id="07bde-131">A [*matrix exponential*](https://en.wikipedia.org/wiki/Matrix_exponential) can also be defined in exact analogy to the exponential function.</span></span>  <span data-ttu-id="07bde-132">矩陣的矩陣指數 $A $ 可以表示為</span><span class="sxs-lookup"><span data-stu-id="07bde-132">The matrix exponential of a matrix $A$ can be expressed as</span></span>

<span data-ttu-id="07bde-133">$ $ e ^ A = \boldone + A + \frac{A ^ 2} {2！}+ \frac{A ^ 3} {3！}+ \cdots $ $</span><span class="sxs-lookup"><span data-stu-id="07bde-133">$$ e^A=\boldone + A + \frac{A^2}{2!}+\frac{A^3}{3!}+\cdots $$</span></span>

<span data-ttu-id="07bde-134">這一點很重要，因為配量機械時間演進是由 $e ^ {iB} $ for Hermitian matrix $B $ 的表單的單一矩陣所描述。</span><span class="sxs-lookup"><span data-stu-id="07bde-134">This is important because quantum mechanical time evolution is described by a unitary matrix of the form $e^{iB}$ for Hermitian matrix $B$.</span></span>  <span data-ttu-id="07bde-135">基於這個理由，執行矩陣指數是量子運算的基本部分，因此問 # 會提供內建常式來描述這些作業。</span><span class="sxs-lookup"><span data-stu-id="07bde-135">For this reason, performing matrix exponentials is a fundamental part of quantum computing and as such Q# offers intrinsic routines for describing these operations.</span></span>
<span data-ttu-id="07bde-136">有許多方法可以在傳統電腦上計算矩陣指數，而在一般的數值將逼近中，這種指數是使用危險太。</span><span class="sxs-lookup"><span data-stu-id="07bde-136">There are many ways in practice to compute a matrix exponential on a classical computer, and in general numerically approximating such an exponential is fraught with peril.</span></span>  <span data-ttu-id="07bde-137">請參閱[*Cleve Moler 和 Charles Van 貸款。「用來計算矩陣指數的十九可疑方法」。SIAM 計算期刊評論20.4 （1978）： 801-836*](https://doi.org/10.1137/S00361445024180) ，取得涉及挑戰的詳細資訊。</span><span class="sxs-lookup"><span data-stu-id="07bde-137">See [*Cleve Moler and Charles Van Loan. "Nineteen dubious ways to compute the exponential of a matrix." SIAM review 20.4 (1978): 801-836*](https://doi.org/10.1137/S00361445024180) for more information about the challenges involved.</span></span>

<span data-ttu-id="07bde-138">若要瞭解如何計算矩陣的指數，最簡單的方式是透過該矩陣的特徵值和特徵向量。</span><span class="sxs-lookup"><span data-stu-id="07bde-138">The easiest way to understand how to compute the exponential of a matrix is through the eigenvalues and eigenvectors of that matrix.</span></span>  <span data-ttu-id="07bde-139">具體而言，上述的 spectral 定理指出每個 Hermitian 或單一矩陣 $A $ 都有一個單一矩陣 $U $ 和對角線矩陣 $D $，因此 $A = U ^ \dagger D U $。</span><span class="sxs-lookup"><span data-stu-id="07bde-139">Specifically, the spectral theorem discussed above says that for every Hermitian or unitary matrix $A$ there exists a unitary matrix $U$ and a diagonal matrix $D$ such that $A=U^\dagger D U$.</span></span>  <span data-ttu-id="07bde-140">由於 unitarity 的屬性，我們有 $A ^ 2 = U ^ \dagger D ^ 2 U $，同樣適用于任何 power $p $ $A ^ p = U ^ \dagger D ^ p U $。</span><span class="sxs-lookup"><span data-stu-id="07bde-140">Because of the properties of unitarity we have that $A^2 = U^\dagger D^2 U$ and similarly for any power $p$ $A^p = U^\dagger D^p U$.</span></span>  <span data-ttu-id="07bde-141">如果我們將此取代為運算子指數的運算子定義，我們會取得：</span><span class="sxs-lookup"><span data-stu-id="07bde-141">If we substitute this into the operator definition of the operator exponential we obtain:</span></span>

<span data-ttu-id="07bde-142">$ $ e ^ A = U ^ \dagger \left （\boldone + D + \frac{D ^ 2} {2！}+ \cdots \right） U = U ^ \dagger \begin{bmatrix}\exp （D_{11}） & 0 & \cdots & 0\\\\ 0 & \exp （D_{22}） & \cdots & 0\\\\ \vdots & \vdots & \ddots & \vdots\\\\ 0& 0 & \cdots & \exp （D_ {NN}） \end{bmatrix} U. $ $</span><span class="sxs-lookup"><span data-stu-id="07bde-142">$$ e^A= U^\dagger \left(\boldone +D +\frac{D^2}{2!}+\cdots \right)U= U^\dagger \begin{bmatrix}\exp(D_{11}) & 0 &\cdots &0\\\\ 0 & \exp(D_{22})&\cdots& 0\\\\ \vdots &\vdots &\ddots &\vdots\\\\ 0&0&\cdots&\exp(D_{NN}) \end{bmatrix} U. $$</span></span>

<span data-ttu-id="07bde-143">換句話說，如果您轉換成矩陣的 eigenbasis $A $，那麼計算矩陣的指數就相當於計算矩陣特徵值的一般指數。</span><span class="sxs-lookup"><span data-stu-id="07bde-143">In other words, if you transform to the eigenbasis of the matrix $A$ then computing the matrix exponential is equivalent to computing the ordinary exponential of the eigenvalues of the matrix.</span></span>  <span data-ttu-id="07bde-144">隨著量子運算中的許多作業都牽涉到執行矩陣指數，轉換成矩陣 eigenbasis 以簡化執行運算子指數的這個技巧，會經常出現，而且是許多量子演算法背後的基礎，例如Trotter –本指南稍後所討論的 Plat'home co. 樣式量子模擬方法。</span><span class="sxs-lookup"><span data-stu-id="07bde-144">As many operations in quantum computing involve performing matrix exponentials, this trick of transforming into the eigenbasis of a matrix to simplify performing the operator exponential appears frequently and is the basis behind many quantum algorithms such as Trotter–Suzuki-style quantum simulation methods discussed later in this guide.</span></span>

<span data-ttu-id="07bde-145">另一個有用的屬性是，如果 $B $ 同時為單一和 Hermitian，也就是 $B = B ^{-1}= B ^ \dagger $，然後 $B ^ 2 = \boldone $。</span><span class="sxs-lookup"><span data-stu-id="07bde-145">Another useful property is if $B$ is both unitary and Hermitian, i.e., $B=B^{-1}=B^\dagger$ then $B^2=\boldone$.</span></span> <span data-ttu-id="07bde-146">藉由將此規則套用至矩陣指數的上方展開，以及將 $ \boldone $ 和 $B $ 詞彙群組在一起，可以看到任何實際值 $x $ 身分識別</span><span class="sxs-lookup"><span data-stu-id="07bde-146">By applying this rule to the above expansion of the matrix exponential and by grouping the $\boldone$ and the $B$ terms together, it can be see that for any real value $x$ the identity</span></span>

<span data-ttu-id="07bde-147">$ $e ^ {iBx} = \boldone \cos （x） + iB\sin （x） $ $</span><span class="sxs-lookup"><span data-stu-id="07bde-147">$$e^{iBx}=\boldone \cos(x)+ iB\sin(x)$$</span></span>


<span data-ttu-id="07bde-148">手.</span><span class="sxs-lookup"><span data-stu-id="07bde-148">holds.</span></span> <span data-ttu-id="07bde-149">這項技巧特別有用，因為它可讓您瞭解動作矩陣指數所擁有的原因，即使 $B $ 的維度是以指數方式表示，當 $B $ 同時為單一和 Hermitian 時，特殊情況也是如此。</span><span class="sxs-lookup"><span data-stu-id="07bde-149">This trick is especially useful because it allows to reason about the actions matrix exponentials have, even if the dimension of $B$ is exponentially large, for the special case when $B$ is both unitary and Hermitian.</span></span>
